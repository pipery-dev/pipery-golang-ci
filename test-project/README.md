# Test Project

This fixture project exists so `pipery-golang-ci` can be executed against a real source tree during `pipery-actions test`.

Replace this directory with a representative source sample for the action.
